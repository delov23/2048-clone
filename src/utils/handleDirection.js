const mapColToRow = (matrix, colIndex) => {};

const removeZeros = (arr) => {};

const sumAlike = (arr) => {};

const padZeros = (arr, direction) => {};

const handleDirection = (direction) => (previousBoard) => {};

export default handleDirection;
